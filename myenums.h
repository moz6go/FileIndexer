#ifndef MYENUMS
#define MYENUMS

enum SearchType {
    BY_NAME = 1,
    BY_EXTANSION,
    BY_DATE,
    BY_SIZE
};

enum Process {
    DEFAULT = 1,
    START,
    STOP,
    PAUSE,
    SEARCH
};

#endif // MYENUMS

